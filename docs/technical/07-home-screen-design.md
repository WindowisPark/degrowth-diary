# 홈 화면 설계 - 몬스터 서식지

## 컨셉

기존 "생산성 앱" 느낌에서 **"몬스터가 사는 공간에 놀러온"** 느낌으로 전환.
기록 리스트가 아닌, 몬스터가 화면의 주인공이 되어야 함.

---

## 화면 구조

```
┌─────────────────────────────────────┐
│ 1월 10일 금요일              ⚙️     │  ← 미니멀 헤더 (48dp)
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐    │
│  │      ░░░ 배경 레이어 ░░░      │    │
│  │   (어둠, 안개, 파티클 등)     │    │
│  │                             │    │
│  │         ┌───────┐           │    │  ← 몬스터 서식지 (55-60%)
│  │         │       │           │    │
│  │         │  👾   │           │    │
│  │         │       │           │    │
│  │         └───────┘           │    │
│  │       "오늘도 배달앱?"       │    │  ← 몬스터 대사
│  │                             │    │
│  │    🔥 7일 연속    Lv.12     │    │  ← 스트릭 + 레벨
│  └─────────────────────────────┘    │
│                                     │
├─────────────────────────────────────┤
│  오늘의 퇴화 (3)           더보기 >  │  ← 기록 섹션 헤더
├─────────────────────────────────────┤
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐   │
│  │ 🍖  │ │ 📱  │ │ 😴  │ │ +2  │   │  ← 기록 칩 (횡스크롤)
│  │야식  │ │폰   │ │수면  │ │    │   │
│  └─────┘ └─────┘ └─────┘ └─────┘   │
├─────────────────────────────────────┤
│                                     │
│         ┌─────────────────┐         │
│         │   + 기록하기    │         │  ← 하단 고정 CTA (56dp)
│         └─────────────────┘         │
│                                     │
└─────────────────────────────────────┘
```

---

## 영역별 상세

### 1. 헤더 (높이: 48dp)

| 요소 | 위치 | 스타일 |
|------|------|--------|
| 날짜 | 좌측 | `1월 10일 금요일` - 친근한 형식 |
| 설정 | 우측 | 아이콘 버튼 (마이페이지 진입) |

- AppBar 사용하지 않음 (커스텀 Row)
- 배경: 투명 또는 서식지와 그라데이션 연결

---

### 2. 몬스터 서식지 (높이: 화면의 55-60%)

핵심 영역. 몬스터가 "살아있는" 느낌을 줘야 함.

#### 2-1. 배경 레이어

```dart
Stack(
  children: [
    // 1) 그라데이션 배경 (어둠 → 약간 밝음)
    Container(gradient: ...),

    // 2) 분위기 파티클 (선택)
    ParticleEffect(), // 먼지, 안개, 별빛 등

    // 3) 바닥 요소 (선택)
    Positioned(bottom: 0, child: GroundDecoration()),
  ],
)
```

**배경 스타일 옵션:**
| 스타일 | 설명 | 분위기 |
|--------|------|--------|
| 동굴형 | 어두운 동굴, 바위 | 미스터리 |
| 늪지형 | 안개, 물웅덩이 | 음습 |
| 실험실형 | 네온 라인, 유리관 | 사이버 |
| 심연형 | 순수 어둠 + 눈만 빛남 | 미니멀 |

#### 2-2. 몬스터 캐릭터

```dart
Positioned(
  // 화면 중앙, 약간 위
  child: Column(
    children: [
      MonsterAvatar(
        monster: mainMonster,
        size: 160,
        // 미묘한 상하 움직임 애니메이션
        animation: BreathingAnimation(),
      ),
      SizedBox(height: 8),
      // 말풍선 (랜덤 대사)
      SpeechBubble(text: monster.randomQuote),
    ],
  ),
)
```

**몬스터 인터랙션:**
- 탭: 도감 상세로 이동
- 롱프레스: 간단한 리액션 (흔들림, 눈 깜빡임)
- 기록 추가 시: 성장 이펙트

#### 2-3. 하단 인포 바

```
    🔥 7일 연속        Lv.12 (230/300 EXP)
```

```dart
Positioned(
  bottom: 16,
  left: 16,
  right: 16,
  child: Row(
    mainAxisAlignment: MainAxisAlignment.spaceBetween,
    children: [
      // 스트릭 뱃지
      StreakBadge(count: 7),
      // 레벨 + EXP 바
      LevelIndicator(level: 12, exp: 230, maxExp: 300),
    ],
  ),
)
```

---

### 3. 오늘의 기록 섹션 (높이: ~120dp)

#### 3-1. 섹션 헤더

```dart
Padding(
  padding: EdgeInsets.symmetric(horizontal: 16, vertical: 8),
  child: Row(
    children: [
      Text('오늘의 퇴화 (3)'),
      Spacer(),
      TextButton(child: Text('더보기'), onPressed: ...),
    ],
  ),
)
```

#### 3-2. 기록 칩 (횡스크롤)

```dart
SizedBox(
  height: 72,
  child: ListView.builder(
    scrollDirection: Axis.horizontal,
    padding: EdgeInsets.symmetric(horizontal: 16),
    itemBuilder: (_, i) => RecordChip(record: records[i]),
  ),
)
```

**RecordChip 디자인:**

```
┌─────────┐
│   🍖    │  ← 카테고리 아이콘 (24dp)
│  야식   │  ← 라벨 (12sp)
│  ●●●○○  │  ← 심각도 표시 (선택)
└─────────┘
    56dp
```

기록이 없을 때:
```
┌───────────────────────────────────┐
│  아직 기록이 없어요               │
│  오늘 뭐 망했어? 👀               │
└───────────────────────────────────┘
```

---

### 4. 하단 CTA (높이: 80dp, 패딩 포함)

```dart
SafeArea(
  child: Padding(
    padding: EdgeInsets.all(16),
    child: SizedBox(
      width: double.infinity,
      height: 56,
      child: ElevatedButton.icon(
        icon: Icon(Icons.add),
        label: Text('기록하기'),
        onPressed: () => navigateToRecord(),
        style: ElevatedButton.styleFrom(
          backgroundColor: AppColors.primary,
          shape: StadiumBorder(),
        ),
      ),
    ),
  ),
)
```

---

## 위젯 구조

```dart
class HomeScreen extends ConsumerWidget {
  @override
  Widget build(context, ref) {
    return Scaffold(
      body: Column(
        children: [
          // 1. 커스텀 헤더
          _HomeHeader(),

          // 2. 몬스터 서식지 (Expanded)
          Expanded(
            flex: 6,  // 60%
            child: _MonsterHabitat(),
          ),

          // 3. 오늘의 기록
          Expanded(
            flex: 2,  // 20%
            child: _TodayRecordsSection(),
          ),

          // 4. 하단 CTA
          _RecordButton(),
        ],
      ),
    );
  }
}
```

---

## 몬스터 없는 상태 (신규 유저)

첫 기록 전에는 몬스터 대신 "알" 또는 "빈 서식지" 표시:

```
┌─────────────────────────────────┐
│                                 │
│           🥚                    │
│     "첫 기록을 하면             │
│      뭔가 태어날지도...?"        │
│                                 │
│                                 │
│       [첫 퇴화 기록하기]         │
│                                 │
└─────────────────────────────────┘
```

---

## 애니메이션 계획

| 요소 | 애니메이션 | 트리거 |
|------|-----------|--------|
| 몬스터 | 미세한 상하 호흡 | 상시 (2초 주기) |
| 몬스터 | 눈 깜빡임 | 3-5초 랜덤 |
| 말풍선 | 페이드 인/아웃 | 10초마다 대사 변경 |
| 기록 칩 | 슬라이드 인 | 화면 진입 시 |
| 파티클 | 천천히 떠다님 | 상시 |
| 레벨업 | 반짝이는 이펙트 | EXP 증가 시 |

---

## 다음 단계

1. [ ] `_MonsterHabitat` 위젯 구현
2. [ ] `_TodayRecordsSection` 위젯 구현
3. [ ] 몬스터 애니메이션 컴포넌트
4. [ ] 빈 상태 (알) UI
5. [ ] 배경 파티클 이펙트 (선택)
